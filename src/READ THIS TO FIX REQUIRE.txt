ERROR CANNOT FIND "require" solved by adding:

declare var require: any

to the top of the ts file.

what a shitty hack to have to do just to import a json file! 
what a bunch of dumbasses to have to force me to hack this together to make it work

find a way to make that import global.