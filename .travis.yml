language: node_js
node_js:
  - "8"
  - "10"

cache: npm

git: 
  depth: 3
before_install:
 - npm i -g @angular/cli
 
jobs:
  include:
    - stage: lint
      script: lint
    - stage: build
      script: bulld-dev
    - stage: test
      script: test-ci
